# ===================================
# CONFIGURAÇÕES DO SISTEMA DE FINANÇAS PESSOAIS
# ===================================

# Configurações do Banco de Dados MongoDB
# Para desenvolvimento local, use: mongodb://127.0.0.1:27017/sistema-financas
# Para produção, use a URL do MongoDB Atlas ou outro provedor
MONGODB_URI=mongodb://127.0.0.1:27017/sistema-financas

# Configurações de Autenticação JWT
# IMPORTANTE: Use uma chave secreta forte em produção
JWT_SECRET=sua_chave_secreta_jwt_muito_segura_aqui_123456789
JWT_EXPIRES_IN=7d

# Configurações do Servidor
# Porta onde o servidor backend irá rodar
PORT=3001

# Ambiente de execução (development, production, test)
NODE_ENV=development

# Configurações de Upload de Arquivos
# Diretório onde os comprovantes serão salvos
UPLOAD_PATH=uploads

# Tamanho máximo do arquivo em bytes (5MB = 5242880 bytes)
MAX_FILE_SIZE=5242880

# Configurações de CORS (opcional)
# Defina as origens permitidas para requisições CORS
# CORS_ORIGIN=http://localhost:5173

# Configurações de Rate Limiting (opcional)
# Número máximo de requisições por IP por janela de tempo
# RATE_LIMIT_MAX=100
# RATE_LIMIT_WINDOW_MS=900000

# Configurações de Log (opcional)
# Nível de log (error, warn, info, debug)
# LOG_LEVEL=info

# ===================================
# INSTRUÇÕES DE CONFIGURAÇÃO
# ===================================
#
# 1. Copie este arquivo para .env
# 2. Substitua os valores de exemplo pelos valores reais
# 3. NUNCA commite o arquivo .env no controle de versão
# 4. Para produção, configure as variáveis no seu provedor de hospedagem
#
# Variáveis obrigatórias:
# - MONGODB_URI: URL de conexão com o MongoDB
# - JWT_SECRET: Chave secreta para assinatura dos tokens JWT
#
# Variáveis opcionais (têm valores padrão):
# - PORT: Porta do servidor (padrão: 3001)
# - NODE_ENV: Ambiente (padrão: development)
# - JWT_EXPIRES_IN: Tempo de expiração do token (padrão: 7d)
# - UPLOAD_PATH: Diretório de uploads (padrão: uploads)
# - MAX_FILE_SIZE: Tamanho máximo do arquivo (padrão: 5MB)